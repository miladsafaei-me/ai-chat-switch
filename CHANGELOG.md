# Changelog

## 1.1.0

- Added the **AI Chats** activity-bar view: one session history merging Claude and
  Codex, grouped by day, opening each conversation in its own agent.
- Added `Ctrl+Alt+S` quick-pick search across both histories.
- Added agent filter, project-scope toggle, refresh, reveal-transcript and
  copy-session-id actions.
- Split the extension into modules; the status-bar switch is unchanged.

## 1.0.0

- Status-bar switch keeping Claude and Codex in one editor group.
